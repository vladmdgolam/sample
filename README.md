# sample

A browser sampler with a pad grid and waveform editor.

Live at [sampler.vladik.xyz](https://sampler.vladik.xyz).

## Develop

```bash
pnpm install
pnpm dev
```

Audio samples live in `public/samples/`.
